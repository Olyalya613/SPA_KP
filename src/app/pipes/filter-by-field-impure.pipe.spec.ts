import { FilterByFieldImpurePipe } from './filter-by-field-impure.pipe';

describe('FilterByFieldImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByFieldImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
